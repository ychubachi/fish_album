= content_for :header do
  %h1= "'#{@keyword}'"

%ul{'data-role' => 'listview'}
  - @fish.each do |fish|
    %li
      = link_to fish_photos_path(fish) do
        %p= fish.name_jp
        %p= fish.name_en
        %p= fish.name_la
